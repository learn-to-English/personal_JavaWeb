<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.learning.dao.UserMapper">

    <!-- 定义 resultMap，用于自动映射数据库列到对象属性 -->
    <resultMap id="userResultMap" type="com.learning.model.User">
        <id column="id" property="id"/>
        <result column="username" property="username"/>
        <result column="password" property="password"/>
        <result column="email" property="email"/>
        <result column="phone" property="phone"/>
        <result column="role" property="role"/>
        <result column="avatar" property="avatar"/>
        <result column="create_time" property="createTime"/>
        <result column="update_time" property="updateTime"/>
    </resultMap>

    <!-- 根据 ID 查询用户 -->
    <select id="selectUserById" parameterType="int" resultMap="userResultMap">
        SELECT * FROM user WHERE id = #{id}
    </select>

    <!-- 根据用户名查询用户 -->
    <select id="selectUserByUsername" parameterType="string" resultMap="userResultMap">
        SELECT * FROM user WHERE username = #{username}
    </select>

    <!-- 查询所有用户 -->
    <select id="selectAllUsers" resultMap="userResultMap">
        SELECT * FROM user ORDER BY create_time DESC
    </select>

    <!-- 插入用户 -->
    <insert id="insertUser" parameterType="com.learning.model.User" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO user (username, password, email, phone, role, avatar, create_time)
        VALUES (#{username}, #{password}, #{email}, #{phone}, #{role}, #{avatar}, NOW())
    </insert>

    <!-- 更新用户 -->
    <update id="updateUser" parameterType="com.learning.model.User">
        UPDATE user
        SET
            username = #{username},
            password = #{password},
            email = #{email},
            phone = #{phone},
            role = #{role},
            avatar = #{avatar},
            update_time = NOW()
        WHERE id = #{id}
    </update>

    <!-- 删除用户 -->
    <delete id="deleteUser" parameterType="int">
        DELETE FROM user WHERE id = #{id}
    </delete>

    <!-- 根据用户名和密码查询用户（用于登录） -->
    <!-- 使用 @Param 注解后，可以直接使用参数名 -->
    <select id="selectUserByUsernameAndPassword" resultMap="userResultMap">
        SELECT * FROM user WHERE username = #{username} AND password = #{password}
    </select>

</mapper>